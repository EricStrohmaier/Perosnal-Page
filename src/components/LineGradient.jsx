// import useColorTheme from "../hooks/ColorTheme";


const LineGradient = ({ width = "w-full" }) => {
  // const {toColor,fromColor, primary} =useColorTheme();
  return <div className={`h-1 ${width}  glass2 `} />;
};

export default LineGradient;
